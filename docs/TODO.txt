[범례] - 문제점 / + 해야 할 것 / = 해결 / ? 미정

26.08.10
= 영상이 소리만 나오고 화면이 안 보이던 버그 수정 — #player-error의 CSS display:flex가
  hidden 속성을 덮어써 빈 오버레이가 플레이어를 항상 가리고 있었음 (:not([hidden])로 해결)
= 홈 화면 개편: 영상 카드 전체 나열 → 플레이리스트 리스트(행) + 상세 화면(#/list/ID) 분리
= 시청 화면에 재생목록 패널 추가 — 플레이어 아래에 전체 곡 목록(현재 곡 강조·자동 스크롤,
  가사/싱크 상태 점 표시), 클릭으로 곡 이동
= 시청 화면 재구성 — 자막/가사입력/싱크 탭을 오른쪽 패널 상단으로 이동, 화면 높이에 맞춘
  고정 레이아웃으로 페이지 스크롤 제거(창이 최소 크기 560px보다 작을 때만 스크롤),
  스크롤바 다크 테마 적용
= 시청 화면 왼쪽 끝에 고정 사이드바 추가 — 홈 이동 + 다른 플레이리스트 전환 (본문은 중앙 유지)
= 나무위키 후리가나(루비)가 복사 시 본문에 섞이는 문제 해결 — 붙여넣기 시 클립보드의
  HTML 서식에서 <rt>만 정확히 제거 (휴리스틱 아닌 마크업 기반)
= 싱크 보정 단위 0.2초 → 0.1초, 시간 배지 클릭 시 1초 전이 아닌 정확히 그 시간부터 재생
= 타임스탬프 소수 첫째 자리로 통일 (기존 데이터 자동 마이그레이션 포함)
= 싱크 탭 개선 — 가사 클릭 = 그 시간부터 재생, 시간 클릭 = 인라인 직접 편집("1:23.4"/"83.4"
  형식), 표시 텍스트 원문/발음/해석 선택(설정 유지), 재생/일시정지 버튼 크기 고정
= 싱크 ±0.1 조절 시 바뀐 시간부터 즉시 재생
= 내보내기/가져오기 확장 — 전체·플레이리스트별 내보내기, 가져오기(추가=병합/교체) 분리
= 공유 데이터 자동 로드 구조 — localStorage가 없으면 data/data.json을 읽음
  (GitHub Pages 배포 준비 완료, 배포 여부는 보류)
= 플레이리스트 실제 유튜브 제목 자동 표시 — RSS 피드를 프록시로 조회, 기존 목록 백필 포함
= "가사 없는 곡"(연주곡) 표시 추가 — 미작업과 구분되는 배지·상태 점, 가사 입력 탭에서 토글
= 내보내기/가져오기/삭제를 홈 화면 도구 막대로 통합 — 내보내기·삭제는 체크박스 선택 방식
  (전체 선택/해제, 선택 개수 표시), 플레이리스트 상세의 중복 버튼 제거
= 프로젝트 시작: 정적 사이트 MVP 구현 — 플레이리스트 가져오기(API 키 불필요), 영상 목록,
  시청 화면(영상+자막), 가사 붙여넣기 자동 파싱(원문/발음/해석), 싱크 편집기(Space 스탬프),
  자막 클릭 → 영상 이동, 현재 줄 자동 강조/스크롤, localStorage 저장 + JSON 백업
= 실제 플레이리스트(요루시카 114곡)로 브라우저 E2E 테스트 통과 (가져오기/파싱/스탬프/클릭 이동)
= 싱크 모드 재생/일시정지 라벨이 영상 전환 시 초기화되지 않던 버그 수정
+ 실제 나무위키 가사 문서 여러 형식으로 파서 검증 (표 형식, 각주 포함 등)
+ 멤버십/임베드 제한 영상의 "재생 제한" 표시 실전 확인 (테스트 플레이리스트에 해당 영상 없었음)
? 로그인/DB(다중 기기 동기화) 도입 여부 — MVP 써본 뒤 결정
